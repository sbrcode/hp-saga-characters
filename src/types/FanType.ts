export default interface FanData {
  name: string,
  species?: string,
  gender?: string,
  wizard?: boolean,
  ancestry?: string,
  hairColour?: string,
  hogwartsStudent?: boolean,
  hogwartsStaff?: boolean,
  alive?: boolean,
}