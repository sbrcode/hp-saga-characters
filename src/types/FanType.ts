export default interface FanData {
  id: string,
  name: string,
  species?: string,
  gender?: string,
  wizard?: boolean,
  ancestry?: string,
  hairColour?: string,
  hogwartsStudent?: boolean,
  hogwartsStaff?: boolean,
  alive?: boolean,
}